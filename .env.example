# Option A: two-legged token (app-level access; server / automation)
APS_CLIENT_ID=your_client_id_here
APS_CLIENT_SECRET=your_client_secret_here

# Option B: three-legged user token (required for user-specific questions like "my hubs")
# Overrides client credentials when set.
# APS_ACCESS_TOKEN=access_token_here
