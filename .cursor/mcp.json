{
  "mcpServers": {
    "autodesk-aps": {
      "command": "uv",
      "args": [
        "--directory",
        "${workspaceFolder}",
        "run",
        "fastmcp",
        "run",
        "server.py"
      ],
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
